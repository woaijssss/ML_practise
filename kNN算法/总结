两个例子可以改变classify0函数参数k的值、训练样本改为随机选取、改变训练样本的数据，都会
对kNN算法的错误率产生影响。

kNN算法是分类数据最简单最有效的算法。
使用前提：必须有接近实际数据的训练样本数据。

特点：执行效率不高
1、必须保存全部数据集，如果训练集很大，必须使用大量的存储空间。
2、由于必须对数据集中的每个数据计算距离值，实际使用时可能会非常耗时。

缺点：
算法无法给出任何数据的基础结构信息，因此我们无法知道平均实例样本和典型实例样本具有什么特征。

k决策树就是kNN的优化版，可以节省大量的计算开销。