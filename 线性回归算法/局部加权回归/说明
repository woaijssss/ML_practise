局部加权回归(LWLR)允许在估计中引入一些偏差，从而降低预测的均方误差
即：给待预测点附近的每个点赋予一定的权重，然后在这个子集上基于最小均方差来进行普通的回归

LWLR使用“核”（与支持向量机中的核类似）来对附近的点赋予更高的权重。核的类型可以自由选择，最常见的核就是高斯核
高斯核对应的权重：x与x(i)越接近，权重值应该越大

局部加权回归的问题：
    增加了计算量，因为它对每个点做预测的时候都需要使用整个数据集；
     计算时，可能会有大多数据点的权重都接近与0。如果避免这些计算将可以减少程序的运行时间，
从而缓解因计算量增加而带来的问题。